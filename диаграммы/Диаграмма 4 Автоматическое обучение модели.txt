@startuml
title Диаграмма 4 Автоматическое обучение модели

start

:Загрузить файл данных;
:Указать target и model;

if (Модель входит в список\nlinear, tree, boosting?) then (да)
  :Прочитать файл в память;
else (нет)
  :Вернуть ошибку 400\n"Неизвестная модель";
  stop
endif

:Запустить run_pipeline(\nfile_bytes, filename,\ntarget, model);

if (Пайплайн выполнен успешно?) then (да)
  :Сформировать JSON-ответ:\nmodel_id, метрики, task, ...;
else (нет)
  if (Ошибка ValueError?) then (да)
    :Вернуть ошибку 422\n"Ошибка валидации данных";
    stop
  else (нет)
    :Вернуть ошибку 500\n"Ошибка обучения";
    stop
  endif
endif

partition "Опционально: действия в интерфейсе" {
  :Показать метрики;
  :Скачать модель\n/download-model/{id};
  :Скачать шаги пайплайна\n/download-steps/{id};

  if (Пользователь авторизован?) then (да)
    :POST /favorite\nСохранить в избранное;
  else (нет)
    :Не сохранять результат\nв избранное;
  endif
}

stop
@enduml