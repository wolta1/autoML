@startuml constructor_workflow
title Диаграмма 8 Работа с визуальным конструктором ML-пайплайна

start

:Перейти на страницу конструктор;


partition "Сборка пайплайна на холсте" {
  :Выбрать тип шага на боковой панели\n(Dataset, Preprocess, Model, Evaluation);
  :Перетащить тип узла на область;
  :Создать узел пайплайна\nв позиции размещения;
  :При необходимости изменить положение узла\n(перетаскивание на холсте);
}

partition "Настройка узла" {
  :Открыть узел;
  :Показать модальное окно «Настройка»;

  if (Тип узла?) then (Dataset)
    :Загрузить файл .csv или .xlsx;
    :Разбор файла на клиенте;
    :Сохранить табличные данные\nи метаданные в атрибутах узла;
  elseif (Preprocess) then
    :Задать масштабирование,\nпропуски, кодирование;
  elseif (Model) then
    :Выбрать тип модели;
  else (прочий)
    :Отобразить заголовок настройки;\n(расширяемая конфигурация);
  endif

  :Подтвердить «Сохранить» в модальном окне;
  :Записать конфигурацию узла\n(JSON в dataset узла);
  :Закрыть модальное окно;
}

partition "Итерация" {
  if (Нужны ещё шаги пайплайна?) then (да)
    :Добавить и настроить\nдополнительные узлы;
  else (нет)
  endif
}

stop
@enduml